import 'package:flutter/material.dart';

import '../previsualization.dart';

class PrevisualizationPage extends StatelessWidget {
  const PrevisualizationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const PrevisualizationView();
  }
}
